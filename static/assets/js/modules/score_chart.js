define(["jquery", "underscore", "d3"], function($, _, d3) {
    var ScoreChart = function ($) {
        function drawChart(container) {

        }

        return {
            drawChart: drawChart
        };
    }();

    return ScoreChart;
});
