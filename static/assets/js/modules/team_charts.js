define(["jquery", "underscore", "d3"], function ($, _, d3) {
    var TeamChart = function ($) {


        function drawChart() {
        }

        return {
            drawChart: drawChart
        };
    }();

    return TeamChart;
});
